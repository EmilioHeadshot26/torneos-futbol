<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Inscribir Equipo</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <%- include('partials/navbar') %>

  <div class="container mt-5">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <% if (message) { %>
          <div class="alert alert-info"><%= message %></div>
        <% } %>

        <div class="card border-primary">
          <div class="card-header bg-primary text-white">
            <h3 class="text-center">
              <i class="fas fa-user-plus me-2"></i>
              Inscribir Equipo en: <%= torneo.nombre %>
            </h3>
          </div>
          <div class="card-body">
            <% if (equipos.length === 0) { %>
              <div class="alert alert-warning">
                <i class="fas fa-exclamation-triangle me-2"></i>
                No hay equipos disponibles para inscribir
              </div>
              <a href="/torneos" class="btn btn-secondary">
                <i class="fas fa-arrow-left me-1"></i> Volver
              </a>
            <% } else { %>
              <form action="/torneos/<%= torneo.id %>/inscribir" method="POST">
                <div class="mb-4">
                  <label class="form-label fs-5">
                    <i class="fas fa-users me-1"></i> Seleccionar Equipo
                  </label>
                  <select class="form-select form-select-lg" name="equipo_id" required>
                    <% equipos.forEach(equipo => { %>
                      <option value="<%= equipo.id %>">
                        <%= equipo.nombre %>
                      </option>
                    <% }); %>
                  </select>
                </div>
                <div class="d-grid gap-2">
                  <button type="submit" class="btn btn-success btn-lg">
                    <i class="fas fa-check-circle me-1"></i> Confirmar Inscripción
                  </button>
                  <a href="/torneos" class="btn btn-outline-secondary">
                    <i class="fas fa-times-circle me-1"></i> Cancelar
                  </a>
                </div>
              </form>
            <% } %>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
</body>
</html>